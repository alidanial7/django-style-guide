"""Generic pure validators (`is_*` → bool).

Add reusable helpers in ``string.py`` (see the commented example there).
Domain rules (password, etc.) belong in ``<app>/validators/``, not here.
"""

__all__: list[str] = []
